import { Button, ButtonPosition } from "./ApplyButton.styled";

const ApplyButton = () => {
  return (
    <ButtonPosition>
      <Button type="button">Apply now</Button>
    </ButtonPosition>
  );
};

export default ApplyButton;
